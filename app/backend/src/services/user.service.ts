import { IUserService } from '../interfaces/ILogin';
// -|> implementando a interface IUserService , todos os metosos que teram na interface IuserService tera que ter na classe
export default class UserService implements IUserService {

}
